[build]
  command = "git submodule update --init --recursive && hugo --gc --minify -b $URL && npx pagefind --site public"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.161.1"
  HUGO_EXTENDED = "true"
  HUGO_ENABLEGITINFO = "true"
  NODE_VERSION = "20"

[context.production.environment]
  HUGO_ENV = "production"

[context.deploy-preview]
  command = "hugo --gc --minify --buildFuture -b $DEPLOY_PRIME_URL && npx pagefind --site public"

[context.branch-deploy]
  command = "hugo --gc --minify -b $DEPLOY_PRIME_URL && npx pagefind --site public"

[[headers]]
  for = "*.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json; charset=UTF-8"
